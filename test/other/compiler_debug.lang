import "std/String"

function main() : int {
	let str : String;
	str.from_c_string("Test String");
	printf("%s", str.ptr); // FIXME: Obviously, this shouldn't be used this way. It doesn't even have a null terminator.
	return 0;
}
